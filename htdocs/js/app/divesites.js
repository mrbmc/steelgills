define(["app"], function($) {
    require(['components/tabs'],function(){

		// SG.map.markers = {/literal}{$DATA.json}{literal};
		// SG.map.scope = {/literal}"{$DISPATCHER.action}"{literal};
		// SG.map.key = "{/literal}{$CONFIG.GOOGLE_API_KEY}{literal}";
		// if(getCookie('tab-map')!="tab-list") {
		// 	SG.map.init();
		// }

    });
});